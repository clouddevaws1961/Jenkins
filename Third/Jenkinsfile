pipeline
{
    agent any
    stages
    {
        stage('Clone repository')
        {
            echo "Clone Repo started"    
            git https://github.com/clouddevaws1961/Jenkins.git
            checkout scm
            echo "Clone Repo Endated"
        }
        stage ('build')
        
            steps {
                echo "Docker Build started"
            }
    }
}